"""
方法論說明頁：三層結構（白話摘要 → 計算公式 → 完整假設清單）+ 版本紀錄。
遵循 FAST Standard（Flexible, Appropriate, Structured, Transparent）。
"""

import streamlit as st


def render_methodology():
    """Render the methodology section with 3 layers + version history."""

    # ============================================================
    # Layer 1: 白話摘要（給決策者）
    # ============================================================
    st.markdown("""
### 計算方法摘要

本模擬器使用**蒙地卡羅方法**進行投資風險評估。核心邏輯：

- **IRR（年化報酬率）**：基於 25 年共 100 季的每季淨現金流，用二分法求解內部報酬率。
  包含建設支出、營運收支、押金收退、殘值回收。
- **崩潰機率**：判定標準為「營運現金 + 緊急準備金」是否足以覆蓋營運需求。
  住戶押金為負債，不計入可動用資金。
- **回本時間**：找到累計現金流「最後一次轉正並持續為正」的時間點。
  排除因押金收入造成的短暫轉正假象。

每次模擬對 9 個關鍵參數進行隨機擾動，重複 1000 次，取中位數和百分位數呈現結果。

> 標有 ⚠️ 的參數為**模型假設**而非實證數據，可在側邊欄「進階模型假設」中調整。
""")

    # ============================================================
    # Layer 2: 計算公式（給分析師）
    # ============================================================
    with st.expander("📐 計算公式詳解", expanded=False):
        st.markdown("""
#### IRR 計算

使用季度現金流的二分法求解：

```
Σ(t=0 to 100) CF_t / (1+r)^t = 0
年化 IRR = (1 + 季度 r)^4 - 1
```

- `CF_0` = -(第一期建設成本 + 營運準備金 + 緊急準備金)
- `CF_t` = 季度營收 - 季度成本 - 季度退費支出（t = 1..99）
- `CF_100` = 同上 + 殘值（在住人數 × 押金 × (1 + 殘值增值率)^25）

> 殘值增值率預設 3%/年 ⚠️ — 基於台灣不動產長期增值率估算

---

#### 崩潰判定

```
可動用資金 = 營運現金 + 緊急準備金（不含押金池）
淨流動性 = 可動用資金 - 待退費積壓
```

觸發條件（任一）：
1. **可動用資金 < 0**（即時崩潰）
2. **淨流動性 < 0 連續超過 N 季**（流動性危機）

> N 預設 = 4 季 ⚠️ — 模型假設，可在進階設定調整

---

#### 回本時間

在累計現金流序列中，從後往前找最後一個 ≤ 0 的時間點。
該時間點的下一季即為「持續回本」的起點。
如果第 100 季的累計現金流 ≤ 0，判定為**永不回本**。

---

#### 品牌信任動態

```
每季加分 = (口碑 + 信託機制 + 文化接受 + 規模效應) × (1 - 信任值/100)
每季衰減 = 基礎衰減 ⚠️ + 老化衰減 × (1 - 對策減免)
```

> 遞減效益：信任越高，同樣的正面因素帶來的加分越少（邊際效應遞減）

---

#### 營運能力均衡

```
均衡點 = 基礎值 ⚠️ + 團隊品質 × 30 - 人員流動率 × 40
每季變化 = (均衡點 - 當前值) × 收斂速度 ⚠️ - 蘇澳罰金
```

---

#### 月費通膨調整

```
月費調整倍率 = 成本通膨累積 ^ 月費漲幅比 ⚠️
```

> 月費漲幅比預設 0.7（成本漲 2% 月費只漲 1.4%），因高齡住戶對漲價敏感
""")

    # ============================================================
    # Layer 3: 完整假設清單（給技術人員/審計者）
    # ============================================================
    with st.expander("📋 完整參數與假設清單", expanded=False):
        st.markdown("""
#### 參數分類

| 符號 | 含義 | 說明 |
|------|------|------|
| 📊 | 使用者輸入 | 在側邊欄可調整 |
| ⚠️ | 模型假設 | 在「進階模型假設」可調整，非實證數據 |
| 📐 | 公式計算 | 由其他參數推導 |

---

#### 核心財務參數

| 參數 | 預設值 | 類型 | 說明 |
|------|--------|------|------|
| 總預算 | 800 億 | 📊 | 依資本結構選擇 |
| 年化資金成本 | 3.5% | 📊 | 依資本結構選擇 |
| 押金金額 | 2,500 萬 | 📊 | 依退還模式選擇 |
| 月費 | 10 萬 | 📊 | 依退還模式選擇 |
| 退費比例 | 90% | 📊 | 依退還模式選擇 |
| 殘值年增值率 | 3% | 📊 | 進階模式可調 |
| 替代投資回報率 | 6% | 📊 | 進階模式可調 |

#### 模型假設參數（⚠️ 進階設定區）

| 參數 | 預設值 | 預設值依據 |
|------|--------|------------|
| 流動性危機門檻 | 4 季 | 模型假設，無行業統一標準 |
| 品牌信任季度衰減 | 0.5 | 模型假設，介於原值 0.3（過低）和 0.8（過高）之間 |
| 月費漲幅/成本通膨比 | 0.7 | 模型假設，基於高齡住戶對漲價敏感的一般認知 |
| 營運能力均衡基礎 | 50 | 模型假設，0-100 量表的中間值 |
| 營運能力收斂速度 | 0.05 | 模型假設，每季修正 5% 的差距 |
| 市場年新進率 | 2% | 📊 基於台灣人口統計（每年約 30 萬人進入 65 歲） |

#### MC 擾動參數（受「整體不確定性水位」控制）

| 被擾動的參數 | 基準 σ | 擾動方式 | 說明 |
|------|--------|----------|------|
| 月費 | 0.05 | 常態 ×(1, σ) | ±15% |
| 人力比 | 0.05 | 常態 ×(1, σ) | ±15% |
| 年度轉化率 | 0.20 | 常態 ×(1, σ) | ±30% |
| 保險轉化強度 | 0.30 | 對數常態 | ±50% |
| H 會館成交率 | 0.40 | 對數常態 | ±50% |
| 建設成本 | 0.10 | 常態 ×(1, σ) | ±20%，台灣營建成本近年波動 10-30% |
| 退費比例 | 0.05 | 常態 ×(1, σ) | ±10% |
| 資金成本 | 0.15 | 常態 ×(1, σ) | ±30%，利率環境變化大時可調高 |

> 「保守」= σ×0.5，「中等」= σ×1.0，「積極」= σ×1.5

#### 經濟環境相關性

參數之間透過共享的「經濟環境因子」建立相關性：
- 月費：economic_shock × 0.03
- 年度轉化率：economic_shock × 0.15
- 保險轉化強度：economic_shock × 0.10

> 經濟差時，入住意願和月費承受力同步下降

#### 人口與退出

| 參數 | 值 | 來源 |
|------|-----|------|
| 入住年齡 | 65-75 歲均勻分佈 | 📐 模型設定 |
| 自願退出率 | 3%/年 | 寫死在 parameters.py |
| 死亡率 | 依年齡查表（1.2%-16%） | 📐 基於精算表 |
| 照護轉移率 | 依年齡查表（2%-15%） | 📐 基於精算表 |
""")

    # ============================================================
    # 版本紀錄
    # ============================================================
    st.markdown("---")
    st.markdown("""
### 計算邏輯版本紀錄

**v2.0（2026-03-26）— 核心計算修正**
- IRR：從 CAGR 簡化公式改為正確的季度現金流 IRR（二分法）
- 崩潰判定：移除押金池（負債不算資產），新增流動性危機判定
- 回本時間：從「首次轉正」改為「最後一次持續轉正」
- 退費支付：修正 operating_cash 為負時的計算 bug
- 新增：收入端通膨調整、品牌信任均衡機制、市場池補充機制
- 新增：MC 擾動範圍擴大（5→9 個參數），加入參數間相關性
- 新增：進階模型假設面板，所有建模假設可調整並標有 ⚠️

**v1.0（初始版本）**
- 使用 CAGR 近似 IRR
- 崩潰判定含押金池
- 回本看首次轉正
- MC 僅擾動 5 個參數
""")
